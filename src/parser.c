#include <stdlib.h>

#include "parser.h"

struct astnode *
do_parsing(struct listnode *tokens)
{
    struct astnode *result;
    struct listnode *current = tokens;

    do
    {
    }
    while (current != NULL);

    return result;
}
